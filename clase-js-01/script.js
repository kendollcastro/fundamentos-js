let edad = 32; //Variable let, su valor puede cambiar. Ejm: La edad puede cambiar.

const pi = 3.1416; //Variable const, su valor no puede cambiar. Ejm: el valor de pi no cambiar.

//Reglas para nombrar variables

//Guión bajo:
let _edad = 28;

//Letra: 
let edadDos = 34;

//Simbolo $:
let $edad = 29;

//Usar nombres descriptivos - Juan tiene 28 / Si queremos usar variables con varias palabras la primer palabra en minuscula.
let edadJuanPrimo = 28;


//---------------------------------------------------------------------------

//Tipos de datos:

//Numero (Number): Almacena numeros enteros o decimales. Ejm:

let price = 2.99;
let quantity = 4;

//Cadena de texto (String): Almacenan texto o caracteres, entre comillas dobles o simples. Ejm:
let name = 'Axel José Guevara Mora - , . _ ? ! # $ %';
let greeting = 'Hola';
let email = 'axeljoseguevara@gmail.com';


//Booleano (Boolean): Representa valores como Verdad o Falso. 
let esMayorDeEdad = true; //True significa verdadero.
let tieneLicencia = false; //False significa falso

//Indefinido (undefined): Es el valor de una variable que ha sido creada pero no se le ha asignado ningún valor.
let age;
console.log(age);